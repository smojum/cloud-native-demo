spring:
  cloud:
    config:
      token: s.0WELFK75fkhIsao8TfiI6LzP
  application:
    name: demo-service
app:
  foo: foo
  bar: bar
server:
  port: 0
management:
  endpoints:
    web:
      exposure:
        include: '*'
eureka:
  instance:
    instanceId: ${spring.application.name}:${vcap.application.instance_id:${spring.application.instance_id:${random.value}}}
