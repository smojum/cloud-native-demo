spring:
  cloud:
    config:
      token: s.JltpLDSxGKMEeuY1TRpYrQ97
  application:
    name: demo-service
app:
  foo: foo
  bar: bar
server:
  port: 8081
#management:
#  endpoints:
#    web:
#      exposure:
#        include: '*'
#eureka:
#  instance:
#    instanceId: ${spring.application.name}:${vcap.application.instance_id:${spring.application.instance_id:${random.value}}}
